@model IEnumerable<FilRouge.Web.ViewModels.DifficultyMasterViewModel>
@{
    ViewBag.Title = "Liste des difficultés Quizz";
}

<h2>Liste des difficultés Quizz</h2>
<p>
    @Html.ActionLink("Create New", "Add")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DifficultyId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DifficultyName)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DifficultyId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DifficultyName)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item. }) |
                @Html.ActionLink("Details", "Detail", new { id = item.DifficultyId })
            </td>
        </tr>
    }

</table>
